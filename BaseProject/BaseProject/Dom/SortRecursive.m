

@interface SortRecursive : NSObject
@end

/*
 
    private func digui(n: Int) -> Int {
        
        int n <= 1 {
            
            return 1
            
            // if n < 1 return 1, 在外面调用就不用判断
        }
        
        return n * digui(n - 1)
    }

*/

@implementation SortRecursive
MakeSourcePath
@end
